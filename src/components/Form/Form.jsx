const Form = () => {
  return (
    <form>
      <input name="name" placeholder="Name" />
      <input name="phone" placeholder="Phone" />
      <button>Add contact</button>
    </form>
  );
};
export default Form;
